<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathmurder.tablehitch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity
            android:name=".LoadingActivity"
            android:label="@string/app_name"
             />
        <activity
            android:name=".ReservationActivity"
            android:label="@string/app_name"
           />
        <activity
            android:name=".MapActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".FoodStallsActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".OrderActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".FoodItemActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".NoSeatActivity"
            android:label="@string/app_name" />
        <activity android:name=".RewardActivity"
            android:label="@string/app_name"></activity>

        <activity android:name=".LoginAndRegisterActivity" android:theme="@style/TestTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>